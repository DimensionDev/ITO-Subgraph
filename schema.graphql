type Seller @entity {
  id: ID!
  address: Bytes!
  name: String!
  message: String!
}

type Buyer @entity {
  id: ID!
  address: Bytes!
}

type Token @entity {
  id: ID!
  type: Int! # 0 - Ether, 1 - ERC20
  address: Bytes!
  name: String
  symbol: String
  decimals: BigInt!
  chain_id: Int!
}

type Pool @entity {
  id: ID!
  chain_id: Int!
  contract_address: Bytes!
  pid: String! # pool id
  password: String!
  hash: String!
  limit: BigInt!
  total: BigInt!
  total_remaining: BigInt!
  seller: Seller!
  start_time: BigInt!
  end_time: BigInt!
  creation_time: BigInt!
  last_updated_time: BigInt!
  token: Token!
  exchange_amounts: [BigInt!]!
  exchange_tokens: [Token!]!
}

type ExchangeInfo @entity {
  id: ID!
  pid: String! # pool id
  buyer: Buyer!
  buy_amount: BigInt!
  buy_time: BigInt!
  buy_token: Token!
}

type PoolInfo @entity {
  id: ID!
  pid: String!
  creation_time: BigInt!
}
